# CyberNova SIEM Configuration
#
# Detection thresholds, severity, and MITRE mappings are defined in
# rules/*.yaml so individual detection rules can be tuned independently.
# This file contains project metadata and risk-scoring thresholds.

project:
  name: CyberNova SIEM Detection Lab
  version: 2.1

risk:
  levels:
    LOW: 20
    MEDIUM: 50
    HIGH: 80
    CRITICAL: 100
